showAddress() {
  ifconfig en0 | grep ether
}

setAddress() {
  openssl rand -hex 6 | sed 's/\(..\)/\1:/g; s/.$//' | xargs sudo ifconfig en0 ether
}


sudo echo ''
echo "Before:" $(showAddress) 
$(setAddress)
echo "After:" $(showAddress)

