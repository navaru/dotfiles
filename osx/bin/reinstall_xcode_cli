#!/usr/bin/env bash

XCODE_SELECT=$(xcode-select --print-path)

echo "Reset xcode-select"
sudo xcode-select --reset
echo "Removing: $XCODE_SELECT"
sudo rm -rf $XCODE_SELECT
echo "Go to System Updates"
